# ⚠️ QUY TẮC BẮT BUỘC CHO AGENT — NGHIÊM CẤM TỰ MÃN

Đọc kỹ trước khi thực hiện bất cứ điều gì hoặc debug tiếp. Vi phạm bất kỳ điều nào dưới đây = báo cáo không được chấp nhận, phải làm lại.

1. **Cấm kết luận "đã fix" / "tối ưu triệt để" / "không còn hiện tượng gì" chỉ dựa trên log số liệu của chính mình.** Người vận hành (Du) quan sát trực tiếp bằng mắt/tai là nguồn sự thật cao hơn log tự diễn giải. Nếu log nói "êm" nhưng người vận hành nói "giật, gầm gừ" → **tin người vận hành, log hoặc cách đo có vấn đề**, không được viết báo cáo mâu thuẫn với quan sát thực tế rồi mặc định log đúng.
2. **Cấm bịa lời giải thích vật lý nghe hợp lý để lấp một hiện tượng chưa hiểu.** Ví dụ đã xảy ra: kết luận "ma sát cơ khí tăng tới 92 Nm" — con số phi thực tế cho một gearbox nhỏ, và đã bị bác bỏ ngay bằng dữ kiện đơn giản (open loop chạy mượt cả ngày cùng tải). Mọi giả thuyết vật lý phải đi kèm **một phép đo độc lập kiểm chứng được** (ví dụ: quay tay để cảm nhận lực cản thật), không được suy diễn từ log điện.
3. **Cấm dùng nhãn/detector nội bộ (ví dụ "phát hiện đột pha") làm bằng chứng mà không show bằng chứng thô đi kèm.** Mỗi lần một detector fire, phải trích đúng timestamp và đối chiếu với raw signal (mech_angle, phase_elec) tại thời điểm đó. Nếu không có bước nhảy tương ứng trong raw signal → phải báo đó là false-positive, không được liệt kê nó như bằng chứng ủng hộ kết luận.
4. **Cấm che triệu chứng bằng hard-clamp mà không chứng minh đó là nguyên nhân gốc.** Mọi giá trị clamp/threshold mới thêm vào (ví dụ Vd≤1.3V) phải nêu rõ: con số này tính từ đâu (mô hình động cơ, đo thực nghiệm cụ thể), và phải có test A/B (có clamp vs không clamp, cùng điều kiện) chứng minh nó thực sự sửa nguyên nhân chứ không chỉ giấu triệu chứng.
5. **Bắt buộc chủ động tìm bằng chứng phản bác giả thuyết của chính mình**, không chỉ tìm bằng chứng ủng hộ. Mỗi test phải nêu rõ: nếu kết quả X thì giả thuyết SAI, nếu kết quả Y thì giả thuyết ĐÚNG — trước khi chạy test, không phải diễn giải sau khi có số liệu.
6. **Mọi thay đổi code phải liệt kê dạng diff cụ thể** (file, dòng, trước/sau), không tóm tắt bằng lời. Người vận hành phải tự đọc lại được toàn bộ patch đã áp dụng trong phiên debug.
7. **Không được xem một lần chạy "êm" là đủ để kết luận đã fix.** Phải chứng minh lặp lại được (ít nhất 3 lần chạy liên tiếp, qua đúng vùng/điều kiện từng fail) trước khi báo cáo là đã giải quyết.
